// ┌─────────────────────────────────────────────────────────────┐
// │  Habit Tracker (4)                        [Экспорт] [Импорт]│  ← title + кнопки
// ├─────────────────────────────────────────────────────────────┤
// │  Фильтры: [только активные сегодня] [скрыть выполненные]   │
// │  Сортировка: [по алфавиту] [по streak] [по дате создания]  │
// ├─────────────────────────────────────────────────────────────┤
// │  🔴 Бегать        streak: 5д    30д: 87%                    │
// │  [01][02][03][04][05][06][07]...[30]          [✏️] [🗑️]    │  ← 30 ячеек
// │                                                             │
// │  🔵 Читать        streak: 12д   30д: 93%                    │
// │  [01][02][03][04][05][06][07]...[30]          [✏️] [🗑️]    │
// │                                                             │
// │  🟢 Медитация     streak: 0д    30д: 40%                    │
// │  [01][02][03][04][05][06][07]...[30]          [✏️] [🗑️]    │
// ├─────────────────────────────────────────────────────────────┤
// │  [+ Добавить привычку]                                      │  ← форма
// │  Название: [        ]  Цвет: [🔴]                           │
// │  Период: [каждый день] [по будням] [пн][вт][ср][чт][пт]    │
// └─────────────────────────────────────────────────────────────┘

// [  ] — день не отмечен (кликабельно)
// [✅] — день отмечен
// [·]  — день не входит в расписание привычки (не кликабельно, серый)
// [>]  — сегодня


// 1. Юзер кликает на день в календаре
//             ↓
// 2. Вызывается action из habitsSlice → toggleCompletion("a1", "2026-05-13")
//             ↓
// 3. Zustand обновляет completions в сторе
//             ↓
// 4. Persist автоматически сохраняет в localStorage
//             ↓
// 5. Все компоненты с селекторами на completions пересчитываются:
//    - календарь перекрашивает день
//    - streak в статистике пересчитывается
//    - процент пересчитывается

// Порядок работы
// Снизу вверх, от данных к UI:

// Типы — описать что такое Habit, Completions, Frequency
// Слайсы стора — habitsSlice (данные) и uiSlice (интерфейс)

// Чистые функции в lib/ — расчёт streak, дней месяца и т.д.
// Каркас UI — расставить блоки на странице через Mantine, пока без логики
// Подключение данных — в каждый блок добавить селектор и нарисовать реальные данные
// Модалка добавления — форма создания привычки
// Фильтры и статистика — селекторы поверх готовых данных
// Шлифовка — persist-миграция, подписка на title, импорт/экспорт
// Что должно работать

// Привычки. 
// Добавление с названием, цветом и периодичностью (каждый день / по будням / по выбранным дням недели). 
// Удаление и переименование.
// Отметка выполнения. Клик по дню календаря отмечает/снимает привычку за этот день.
// Статистика (производные значения, считай в селекторах):


// лучший streak за всё время;


// Фильтры. 
// «показывать только активные сегодня»,
// «скрыть выполненные сегодня», сортировка по streak / по алфавиту / по дате создания.

// Персистентность. При перезагрузке страницы всё сохраняется. Сохранять только сами привычки и отметки, но не UI-фильтры.

// Технические требования



// Кнопка «Импорт/Экспорт JSON» — getState() для экспорта, setState(parsed, true) для импорта (полная замена).
// Селекторы для статистики должны быть мемоизированы — либо через useShallow, либо вычислять внутри селектора без создания новых объектов на каждом рендере.
// Подпишись вне React: при изменении количества привычек обновляй document.title (например, «Habit Tracker (4)»). Используй subscribeWithSelector.




// Чек-лист самопроверки

// При вводе названия новой привычки список привычек не перерендеривается (правильные селекторы).
// Удаление привычки через DevTools setState сразу отражается в UI.
// После localStorage.clear() и перезагрузки стор стартует с дефолта.
// Импорт чужого JSON полностью заменяет состояние.
// document.title меняется без вызова setState в компоненте.